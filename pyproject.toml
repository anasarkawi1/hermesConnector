[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "portunusConnector"
version = "0.0.1b003"
dependencies = [
    "numpy",
    "pandas",
    "binance-connector",
    "dataclasses"
]