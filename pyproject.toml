[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hermesConnector"
version = "0.0.1"
dependencies = [
    "numpy",
    "pandas",
    "binance-connector",
    "dataclasses"
]